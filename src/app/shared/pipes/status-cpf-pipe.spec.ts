import { StatusCpfPipe } from './status-cpf-pipe';

describe('StatusCpfPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusCpfPipe();
    expect(pipe).toBeTruthy();
  });
});
